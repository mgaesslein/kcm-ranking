# Database
DATABASE_URL="postgresql://kcmranking:password@localhost:5432/kcm_ranking?schema=public"

# Server
PORT=3001
NODE_ENV=development

# API
API_PREFIX=/api

# API Security
# Generate strong API keys using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Or: openssl rand -hex 32
API_KEYS=your-secret-api-key-1,your-secret-api-key-2

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173,https://yourdomain.com
# Note: All Chrome extensions are allowed by default (no need to whitelist extension IDs)

